const driveTypes = [
  "Four wheel drive ",
  "Front wheel drive ",
  "Rear wheel drive ",
];

const driveTypeData = driveTypes.map((driveType) => {
  return {
    title: driveType,
  };
});

export default driveTypeData;
